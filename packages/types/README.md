# @rythra/types

Shared framework-agnostic public contracts used across Rythra packages.

Types are kept separate so connectors, protocol implementations and integrations can depend on stable contracts without importing the full runtime.
